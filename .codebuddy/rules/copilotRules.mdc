---
description: AI助手工作流程和规范
alwaysApply: true
enabled: true
enabled: true
updatedAt: 2026-02-13T10:02:54.194Z
provider: 
---

# AI 助手工作规则

## 概述

本文档定义了 AI 助手在项目开发过程中必须遵循的工作流程和规范，确保开发过程有序、可追溯、高质量。

---

## 1. 需求处理流程

### 1.1 需求讨论阶段
- 【必须】接收到需求后，**禁止直接修改任何文件**
- 【必须】与用户充分讨论，明确需求范围、边界条件、预期产出
- 【必须】确认技术方案可行性，识别潜在风险和依赖

### 1.2 任务规划阶段
- 【必须】讨论完成后，按照 `memory-bank/tasks/TASK_TEMPLATE.md` 格式创建任务文档
- 【必须】为每个独立的功能模块或需求创建单独的 Task
- 【必须】将新 Task 添加到 `memory-bank/tasks/_index.md` 对应状态区域
- 【必须】Task ID 按顺序递增，格式为 `TASKXXX`（如 TASK004、TASK005）

---

## 2. 分支管理规范

### 2.1 分支命名
| 类型 | 格式 | 示例 |
|------|------|------|
| 新功能 | `feature/TASKXXX-short-description` | `feature/TASK004-add-cosine-space` |
| Bug修复 | `fix/TASKXXX-short-description` | `fix/TASK005-fix-memory-leak` |
| 重构 | `refactor/TASKXXX-short-description` | `refactor/TASK006-optimize-hnsw` |
| 紧急修复 | `hotfix/short-description` | `hotfix/critical-segfault` |

### 2.2 分支创建
- 【必须】接收到可执行指令后，先创建新分支再开始改动
- 【必须】分支从 `main` 分支创建（紧急修复除外）
- 【推荐】开始工作前先 `git pull` 确保本地代码最新

---

## 3. 任务执行规范

### 3.1 开发流程
```
创建分支 → 实现子任务 → 编译验证 → 更新Task文档 → Commit → Push → 完成所有子任务 → 运行测试 → 提交PR
```

### 3.2 子任务完成标准
- 【必须】代码可编译通过（无编译错误和警告）
- 【必须】更新 `memory-bank/tasks/TASKXXX.md` 中的子任务状态和进度日志
- 【必须】Commit 并 Push 到远端，防止进度丢失

### 3.3 Task完成标准
- 【必须】所有子任务已完成
- 【必须】所有测试通过（包括新增和已有测试）
- 【必须】新增代码有单元测试覆盖
- 【必须】更新 Task 状态为「已完成」
- 【必须】提交 Pull Request

---

## 4. Commit 规范

### 4.1 Commit Message 格式
```
<type>(<scope>): <subject>

<body> (可选)
```

### 4.2 Type 类型
| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(space): add cosine similarity space` |
| `fix` | Bug修复 | `fix(hnsw): fix neighbor selection edge case` |
| `test` | 添加/修改测试 | `test(bruteforce): add edge case tests` |
| `refactor` | 重构（不改变功能） | `refactor(datastore): simplify interface` |
| `docs` | 文档更新 | `docs(readme): update build instructions` |
| `chore` | 构建/工具变动 | `chore(cmake): update compiler flags` |

### 4.3 Commit 要求
- 【必须】每次提交前确保项目可编译通过
- 【必须】Subject 不超过50字符，使用祈使语气
- 【必须】一个 Commit 只做一件事，避免大而全的提交

---

## 5. Pull Request 规范

### 5.1 PR 标题格式
```
[TASKXXX] 简短描述
```
示例：`[TASK004] 添加余弦相似度空间支持`

### 5.2 PR 描述模板
```markdown
## 关联任务
- Task: TASKXXX

## 变更内容
- [ ] 子任务1描述
- [ ] 子任务2描述

## 测试情况
- 新增测试：xxx
- 测试通过情况：xxx

## 检查清单
- [ ] 代码可编译
- [ ] 所有测试通过
- [ ] 新增代码有单测覆盖
- [ ] Task文档已更新
```

### 5.3 PR 合并前检查
- 【必须】所有 CI 检查通过
- 【必须】代码审查通过（如有）
- 【必须】无合并冲突

---

## 6. PR 合并后处理

### 6.1 必须更新
- 【必须】更新 `memory-bank/tasks/_index.md`，将 Task 移至「已完成」区域
- 【必须】更新 `memory-bank/progress.md`，记录已完成功能和当前状态
- 【必须】如有新增技术决策，更新 `memory-bank/techContext.md`
- 【必须】如有新增系统模式，更新 `memory-bank/systemPatterns.md`

### 6.2 分支清理
- 【推荐】合并后删除特性分支
- 【推荐】本地同步最新 main 分支

---

## 7. 特殊情况处理

### 7.1 紧急修复（Hotfix）
- 允许跳过部分规划流程，但仍需：
  1. 创建 hotfix 分支
  2. 修复后提交 PR
  3. 事后补充 Task 文档

### 7.2 任务阻塞
- 【必须】在 Task 文档中记录阻塞原因、影响范围
- 【必须】更新 Task 状态为「阻塞」
- 【推荐】记录可能的解决方案或替代方案

### 7.3 计划变更
- 【必须】与用户讨论确认变更内容
- 【必须】更新 Task 文档的实现计划和子任务列表
- 【必须】在进度日志中记录变更原因

### 7.4 回滚处理
- 【必须】通过新增 Revert PR 回滚，禁止 force push
- 【必须】在 Revert PR 中说明回滚原因

---

## 8. 测试覆盖要求

### 8.1 覆盖率标准
- 【必须】新增核心逻辑代码测试覆盖率 ≥ 80%
- 【推荐】关键算法模块测试覆盖率 ≥ 90%

### 8.2 测试范围
- 【必须】正常路径测试
- 【必须】边界条件测试
- 【推荐】异常情况测试
- 【推荐】性能基准测试（对性能敏感模块）

---

## 9. 快速参考流程图

```
需求接收
    │
    ▼
讨论确认（不修改文件）
    │
    ▼
创建 Task 文档 → 更新 _index.md
    │
    ▼
收到执行指令
    │
    ▼
创建分支 ─────────────────────────┐
    │                              │
    ▼                              │
循环：实现子任务                    │
    │                              │
    ├─► 编译验证                    │
    │                              │
    ├─► 更新 Task 文档              │
    │                              │
    ├─► Commit & Push              │
    │                              │
    ▼                              │
运行完整测试                        │
    │                              │
    ▼                              │
提交 PR ◄──────────────────────────┘
    │
    ▼
PR 合并
    │
    ▼
更新 memory-bank 文档
```
