// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/debian
{
	"name": "ghcr_universal",
	// Use custom dockerfile (not working)
	// "build": {
	// 	"dockerfile": "Dockerfile.codespace",
	// 	"args": {
	// 		// Default to GHCR image owned by the repository owner. Replace if needed.
	// 		"BASE_IMAGE": "ghcr.io/${{ github.repository_owner }}/arena-hnswlib-base:latest"
	// 	}
	// },
	// Or use an image. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/universal:linux",
	// "features": {
	// 	"ghcr.io/ttyrho/devcontainer-features/gcc:1": {},
	// 	"ghcr.io/devcontainers-extra/features/cmake:1": {},
	//	"ghcr.io/devcontainers/features/common-utils:2": {},
	//	"ghcr.io/devcontainers/features/git:1": {},
	//	"ghcr.io/devcontainers/features/git-lfs:1": {},
	// }

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	// Configure tool-specific properties.
	"customizations": {
		"settings": {
			"editor.tabSize": 4
		}
	}

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "vscode"
}
